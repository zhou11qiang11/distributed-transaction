<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.zq.dao.QgGoodsMapper">


    <update id="lockStock">
        UPDATE qg_goods
        SET stock=stock-#{num},
        lockStock=lockStock+#{num}
        WHERE id=#{id} AND stock>=#{num}
    </update>
    <update id="lockStockConfirm">
        UPDATE qg_goods
        SET lockStock=lockStock-#{num}
        WHERE id=#{id}
    </update>
    <update id="lockStockCancel">
        UPDATE qg_goods
        SET stock=stock+#{num},
        lockStock=lockStock-#{num}
        WHERE id=#{id}
    </update>
</mapper>