<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "mybatis-3-mapper.dtd">
<mapper namespace="cn.zq.dao.QgOrderMapper">

    <insert id="insert">

        INSERT INTO qg_order.qg_order
        (
        userId,
        stockId,
        orderNo,
        goodsId,
        num,
        amount,
        STATUS,
        aliTradeNo,
        createdTime,
        updatedTime
        )
        VALUES
        (
        #{userId},
        #{stockId},
        #{orderNo},
        #{goodsId},
        #{num},
        #{amount},
        #{status},
        #{aliTradeNo},
        #{createdTime},
        #{updatedTime}
        );

    </insert>

</mapper>